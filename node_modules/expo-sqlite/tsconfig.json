// tsconfig.json

{
  // ❌ ANTES (Causa do erro de arquivo não encontrado)
  // "extends": "expo-module-scripts/tsconfig.base", 

  // ✅ DEPOIS (Use o base padrão do Expo)
  "extends": "expo/tsconfig.base", 
  
  "compilerOptions": {
    "outDir": "./build"
  },
  "include": ["./src"],
  "exclude": ["**/__mocks__/*", "**/__tests__/*", "**/__stories__/*"]
}